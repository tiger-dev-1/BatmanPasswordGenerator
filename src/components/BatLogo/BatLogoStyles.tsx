import { StyleSheet } from 'react-native';

export const batLogoStyles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontSize: 18,
    color: '#E5BF3C',
    fontWeight: 'bold',
  },
  logo: {
    resizeMode: 'contain',
    height: 180,
  }
});